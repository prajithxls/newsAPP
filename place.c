#include<stdio.h>
int main()
{
    int n1=5,n2=10,sum;
    sum=add(n1,n2);
    printf("the sum %d\n",sum);
    return 0;
}
int add(int a, int b);
int mul(int a, int b);

int main();
{
    int n1=5,n2=10,sum,m;
    sum=add(n1,n2);
    m=mul
}